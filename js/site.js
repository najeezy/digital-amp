// Generated by CoffeeScript 1.8.0
(function() {
  var orderImages, scrollActions, scrollToSection;

  $(function() {
    orderImages();
    $(window).on('scroll', scrollActions);
    return $('.navbar a').on('click', scrollToSection);
  });

  orderImages = function() {
    return setInterval(function() {
      return $('.image-container:last').prependTo('#intro-container');
    }, 15000);
  };

  scrollActions = function(event) {
    var $nav;
    if ($(window).scrollTop() >= 200) {
      $nav = $('nav:first');
      $nav.addClass('scrolled');
    } else {
      $nav = $('nav:first');
      $nav.removeClass('scrolled');
    }
    if ($(window).scrollTop() >= 600) {
      $('.icons:first p').addClass('shown');
    }
    if ($(window).scrollTop() >= 1650) {
      return $('#services li').addClass('shown');
    }
  };

  scrollToSection = function(event) {
    var target;
    event.preventDefault();
    if (this.hash) {
      target = $(this.hash);
    }
    return $('html, body').animate({
      scrollTop: target.offset().top
    }, 500);
  };

}).call(this);
